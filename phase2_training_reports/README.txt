To execute the code properly:
	Need 1 file for the data(included in the folder):
		- all_features_all_data.csv
	To run:
		`python3 aqi_model(2).py'
		- just need to run the script as you would a normal python file
		- this is for the entire pipeline, the autoencoder was trained separate to get results, and was added to the pipeline after
		- MUST CHANGE FILE PATH IN CONFIG CLASS to current path of csv
		
		
